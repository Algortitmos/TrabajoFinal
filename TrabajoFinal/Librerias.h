#include <iostream>
#include <string>
#include <functional>
#include <vector>
#include <fstream>
#include <stdlib.h>

using std::cin;
using std::cout;
using std::endl;
using std::string;
using std::vector;
using std::ifstream;
using std::function;
using std::ofstream;
using std::time;
using std::ios;
using std::pair;
using std::to_string;
using std::getline;

using namespace System;